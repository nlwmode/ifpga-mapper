# fpga-map-tools/src

we integrated the fpga-map-tool command ifga into yosys, so we can using ifpga for fpga mapping in yosys instead of abc.

```
  1. we fulfill the ifpga command in iFPGA
  2. we integrated the command ifpga into yosys
```





